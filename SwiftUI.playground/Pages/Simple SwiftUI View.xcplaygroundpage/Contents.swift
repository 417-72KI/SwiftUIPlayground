import SwiftUI
import PlaygroundSupport

struct SampleView: View {
    var body: some View {
        VStack {
            Text("Hello world!")
        }
    }
}

PlaygroundPage.current.setLiveView(SampleView())
